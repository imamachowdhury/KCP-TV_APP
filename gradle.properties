# Project-wide Gradle settings.

# AndroidX libraries use this property to determine whether to use the AndroidX version of a library.
android.useAndroidX=true

# Performance improvement for large projects.
# This replaces the deprecated 'android.dependency.excludeLibraryComponentsFromConstraints'
android.dependency.useConstraints=false
org.gradle.configuration-cache=true
